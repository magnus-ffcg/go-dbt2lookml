name: 'sample_dbt_project'
version: '1.0.0'
config-version: 2

profile: 'sample_project'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sample_dbt_project:
    +materialized: table
    +tags:
      - looker
